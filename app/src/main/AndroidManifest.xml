<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tw.edu.fcu.vegetablemaster">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".VegetableList" />
        <activity
            android:name=".HomePage"
            android:label="@string/title_activity_homepage"
            android:theme="@style/AppTheme.NoActionBar"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SuggestVeg"
            android:label="今日推薦蔬菜"/>
        <activity android:name=".HistoryVeg"
            android:label="歷史菜價查詢"/>
        <activity android:name=".ForecastVeg"
            android:label="Forecast Vegetable"/>
        <activity android:name=".CompareVeg"
            android:label="Compare Vegetable"/>
        <activity android:name=".Recipe"
            android:label="Recipe"></activity>
    </application>

</manifest>